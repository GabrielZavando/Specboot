# Tasks: Restrict package.json to framework-only distribution

## Phase 1: Rewrite package.json
- [x] Rewrite `package.json`: `files` allowlist (framework-only), `bin`, `scripts`, `description`, `keywords`; keep `version` 0.1.1 and `publishConfig.registry` (High | Infrastructure | 1h)
    - Suggested Path: `package.json`
    - Test Path: `npm pack --dry-run` (assert listing)

## Phase 2: Reconcile .npmignore
- [x] Remove blanket `.github/` and `.opencode/` exclusions; add `.openspec/`; keep openspec/, tests/, node_modules/, .git/, .env*, CHANGELOG.md, logs (Medium | Infrastructure | 0.5h)
    - Suggested Path: `.npmignore`

## Phase 3: Keep shipped CLI self-consistent
- [x] Remove `"update.sh"` from `specboot.sh` REQUIRED_FILES array (High | Infrastructure | 0.25h)
    - Suggested Path: `specboot.sh`

## Phase 4: Capability spec (generated by plan-change)
- [x] `openspec/changes/restrict-package-files/specs/npm-distribution/spec.md` written as a delta (MODIFIED "Package configuration", ADDED "Reconciled .npmignore" + "Self-consistent shipped CLI"). `/apply` MUST satisfy REQ-1..REQ-6 against it. (Medium | Docs | done)
    - Suggested Path: `openspec/changes/restrict-package-files/specs/npm-distribution/spec.md`

## Phase 5: Verification
- [x] `npm pack --dry-run` lists only allowlisted files; assert absences (High | Infrastructure | 0.5h)
- [x] `bash check-refs.sh` exits 0 (High | Infrastructure | 0.25h)
- [x] `bash specboot.sh --ci` exits 0 (High | Infrastructure | 0.25h)
- [x] `openspec validate restrict-package-files` passes (High | Docs | 0.25h)
